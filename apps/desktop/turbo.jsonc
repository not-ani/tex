{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist-electron/**"]
    },
    "start": {
      "dependsOn": ["build", "@tex/web#build"],
      "cache": false,
      "persistent": true
    }
  }
}
